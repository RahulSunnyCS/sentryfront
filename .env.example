# Backend API base URL.
# If unset, the app runs entirely on fixture data (no real scans).
NEXT_PUBLIC_API_URL=

# Optional: override the default accent theme (teal | indigo | violet)
NEXT_PUBLIC_DEFAULT_ACCENT=teal
