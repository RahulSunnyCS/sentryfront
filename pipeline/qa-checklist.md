# QA Checklist

Generated by: qa-planner
Plan context: Bootstrap Playwright E2E framework, wire CI, add critical-path landing-page tests for /en (landing-hero.tsx)
Risk level: MEDIUM
Auth/PII escalated: no

---

## Landing Page Render

🔴 The landing page renders its core elements without errors
  Scenario    : Navigate to /en with no mocks beyond the stats endpoint stub. Assert the hero heading is visible, #hero-url input is present, the submit button is present, the primary nav is present, and the footer is present. Capture browser console and page-level errors during the render pass.
  Pass if     : All five elements are present in the DOM; zero console errors and zero page-level JS errors are observed before any interaction.
  Fail if     : Any of the five elements is absent, or one or more console/page errors are emitted during the initial render.
  Automatable : yes
  @critical

🟢 The page title and meta description are set correctly for the /en locale
  Scenario    : Navigate to /en and read document.title and the content of meta[name=description].
  Pass if     : Title is non-empty and contains the product name; meta description is non-empty.
  Automatable : yes
  @non-blocker

---

## Scan-Submit Critical Path

🔴 Submitting a URL in the hero form navigates to the scan result page
  Scenario    : Mock POST /api/v1/scans to return { id: "test-scan-001" } with HTTP 200. Mock GET /api/v1/scans/test-scan-001, GET /api/v1/scans/test-scan-001/findings, and GET /api/v1/scans/test-scan-001/events defensively (return minimal stubs so the destination page does not 500). Fill #hero-url with a well-formed URL (e.g. https://example.com), submit the form, and wait for the URL to change.
  Pass if     : Browser URL matches the pattern /\/en\/scan\/[^/?]+/ within the configured navigation timeout.
  Fail if     : URL does not transition to /en/scan/<id> within the timeout, or a network/JS error prevents form submission.
  Automatable : yes
  @critical

🔴 The scan-submit POST request carries the correct payload
  Scenario    : Same route mocks as above. Intercept the POST /api/v1/scans request. Fill #hero-url with https://example.com and submit.
  Pass if     : The intercepted request body contains { url: "https://example.com" } (or the URL the user entered).
  Fail if     : The request body is missing, malformed, or contains a different URL than what was entered.
  Automatable : yes
  @critical

🟡 Submitting with an empty input defaults to example.com and navigates to the scan result page
  Scenario    : Mock POST /api/v1/scans to return { id: "test-scan-002" }. Stub the three /scans/:id* GETs. Clear #hero-url so it is empty. Submit the form.
  Pass if     : The intercepted POST body contains { url: "example.com" } and the browser URL transitions to /en/scan/test-scan-002.
  Fail if     : POST body contains an empty or null URL, or the URL transition does not occur.
  Automatable : yes
  @functional

---

## Navigation and Routing

🔴 Navigating to / redirects to /en
  Scenario    : Navigate directly to http://localhost:3000/. Wait for navigation to settle.
  Pass if     : Final URL is http://localhost:3000/en (or starts with /en).
  Fail if     : URL remains / or resolves to a 404 or error page.
  Automatable : yes
  @critical

🟡 The final-CTA pricing link navigates to /en/pricing
  Scenario    : On /en, locate the final call-to-action link that points to pricing (by role=link with accessible name containing "pricing" or equivalent). Click it.
  Pass if     : Browser URL transitions to /en/pricing (or a URL matching that path) without an error page.
  Fail if     : Link is absent, does not navigate, or lands on an error page.
  Automatable : yes
  @functional

---

## Weekly Counter (Stats Widget)

🟢 The weekly scan counter displays the value returned by the stats endpoint
  Scenario    : Mock GET /api/v1/stats/scan-count to return { count: 4321 } with HTTP 200. Navigate to /en and wait for the counter element to appear.
  Pass if     : The counter element's text content contains "4321" (or a formatted equivalent, e.g. "4,321").
  Automatable : yes
  @non-blocker

🟢 The weekly scan counter is present when the stats endpoint returns zero
  Scenario    : Mock GET /api/v1/stats/scan-count to return { count: 0 }. Navigate to /en.
  Pass if     : Counter element renders without a JS error; shows "0" or equivalent.
  Automatable : yes
  @non-blocker

🟢 The FAQ first accordion item is open by default
  Scenario    : Navigate to /en and locate the first FAQ item element (by role=button or role=listitem in the FAQ section). Inspect its expanded state without any user interaction.
  Pass if     : The first FAQ item is expanded or its content panel is visible on initial load.
  Automatable : partial
  @non-blocker

---

## Test Infrastructure — Vitest / Playwright Isolation

🔴 Vitest does not pick up Playwright spec files
  Scenario    : With the e2e/ directory present and containing at least one *.spec.ts file, run npm run test (Vitest). Observe which files are collected.
  Pass if     : Vitest collects zero files from e2e/**; the unit test run completes without Playwright-related import errors or failures.
  Fail if     : Vitest attempts to run any file under e2e/, producing import errors, test failures, or a non-zero exit code attributable to e2e specs.
  Automatable : yes
  @critical

🔴 The test:e2e npm script exists and is wired to Playwright
  Scenario    : Inspect package.json scripts. Confirm a "test:e2e" key is present. Run npm run test:e2e --help (or equivalent dry-run) to confirm the script resolves to the playwright CLI without executing tests.
  Pass if     : "test:e2e" key exists in package.json and resolves to "playwright test".
  Fail if     : "test:e2e" is absent from package.json, or it resolves to a different binary.
  Automatable : yes
  @critical

🟡 The test:e2e:ui npm script exists alongside test:e2e
  Scenario    : Inspect package.json scripts for a "test:e2e:ui" key.
  Pass if     : "test:e2e:ui" is present and its value contains "--ui".
  Fail if     : Key is absent.
  Automatable : yes
  @functional

🔴 playwright.config.ts exists at the repo root and references the e2e/ test directory
  Scenario    : Read playwright.config.ts from the repo root. Assert testDir is set to "./e2e" and baseURL is set to "http://localhost:3000".
  Pass if     : Both values are present and correct.
  Fail if     : File is absent, testDir points elsewhere, or baseURL is missing.
  Automatable : yes
  @critical

🟡 playwright.config.ts enforces workers:1, retries in CI, and forbidOnly in CI
  Scenario    : Read playwright.config.ts. Check workers, retries, and forbidOnly settings.
  Pass if     : workers is 1; retries is 1 when CI env var is set and 0 otherwise; forbidOnly is truthy only in CI.
  Fail if     : workers > 1; retries or forbidOnly are unconditionally set (not environment-gated).
  Automatable : yes
  @functional

🟡 The e2e/support/mocks.ts shared helper exists and is imported by all e2e tests
  Scenario    : Inspect the e2e/ directory for a support/mocks.ts file. Verify that each test file imports from it for the three route stubs (POST /api/v1/scans, GET /api/v1/stats/scan-count, GET /api/v1/scans/:id*).
  Pass if     : File exists; all three mocks are defined in it; every e2e spec that uses those routes imports from this module.
  Fail if     : File is absent, or any spec duplicates mock logic inline instead of using the shared helper.
  Automatable : partial
  @functional

---

## Test Infrastructure — package-lock.json and Dependency Pinning

🔴 @playwright/test is pinned to exact version 1.59.1 (no caret or tilde)
  Scenario    : Inspect package.json devDependencies for the @playwright/test entry.
  Pass if     : Value is exactly "1.59.1" (no leading ^ or ~).
  Fail if     : Entry is absent, or the version string contains ^ or ~.
  Automatable : yes
  @critical

🔴 package-lock.json has been regenerated and reflects the @playwright/test addition
  Scenario    : Inspect package-lock.json for an entry resolving @playwright/test at 1.59.1. Confirm lockfileVersion is 3.
  Pass if     : @playwright/test appears in package-lock.json at 1.59.1; lockfileVersion is 3.
  Fail if     : Entry is absent from the lockfile, or the lockfile version is not 3.
  Automatable : yes
  @critical

🟢 yarn.lock has not been modified by the dependency change
  Scenario    : Run git diff HEAD -- yarn.lock (or equivalent) after the change.
  Pass if     : yarn.lock shows no modifications.
  Automatable : yes
  @non-blocker

---

## CI — GitHub Actions e2e Job

🔴 The CI e2e job is defined in .github/workflows/test.yml
  Scenario    : Read .github/workflows/test.yml and locate a job named (or logically equivalent to) "e2e". Confirm it runs npm run test:e2e.
  Pass if     : A distinct e2e job exists in the workflow; it includes npm run test:e2e as a step.
  Fail if     : No e2e job is present, or the existing test/build jobs are the only jobs defined.
  Automatable : yes
  @critical

🔴 The CI e2e job uses Node 20.19 and runs npm ci before test execution
  Scenario    : Inspect the e2e job steps in test.yml.
  Pass if     : node-version is 20.19 (or equivalent); npm ci is a step that precedes npm run test:e2e.
  Fail if     : A different Node version is used, or npm ci is absent (meaning the lockfile would be ignored).
  Automatable : yes
  @critical

🟡 The CI e2e job caches the Playwright browser binary
  Scenario    : Inspect the e2e job for a cache step targeting ~/.cache/ms-playwright keyed on the Playwright version.
  Pass if     : A cache step is present with the correct path and a cache key that includes the Playwright version string.
  Fail if     : No cache step is present (not a hard blocker, but causes slow CI and is a plan requirement).
  Automatable : yes
  @functional

🟡 The CI e2e job uploads the playwright-report/ artifact on both pass and failure
  Scenario    : Inspect the e2e job for an upload-artifact step that references playwright-report/ with if: always().
  Pass if     : Step is present, path includes playwright-report/, and the condition is always().
  Fail if     : Step is absent or condition is omitted (reports not uploaded on failure → no debugging data).
  Automatable : yes
  @functional

🟡 The CI e2e job installs Chromium with system dependencies via npx playwright install
  Scenario    : Inspect the e2e job steps for a playwright install step.
  Pass if     : Step runs npx playwright install --with-deps chromium (or equivalent).
  Fail if     : Step is absent or installs all browsers (wasteful) or lacks --with-deps (binary may fail on the CI runner).
  Automatable : yes
  @functional

🟡 The existing test and build CI jobs are unchanged after the e2e job addition
  Scenario    : Diff the test and build job definitions in test.yml against the pre-change baseline (or read and verify their structure).
  Pass if     : No modifications to the existing test or build job steps, Node versions, or triggers.
  Fail if     : Any existing job step or setting has been altered.
  Automatable : partial
  @functional

---

## .gitignore Coverage

🟢 Generated E2E artifacts are excluded from version control
  Scenario    : Inspect .gitignore for entries covering /test-results/, /playwright-report/, /blob-report/, /playwright/.cache/, and e2e.db*.
  Pass if     : All five patterns are present.
  Automatable : yes
  @non-blocker

---

TIER SUMMARY
🔴 Critical    : 13 — all must pass at Automation Gate for Gate 2 to pass
🟡 Functional  : 10 — failures -> CONDITIONAL PASS at Gate 2
🟢 Non-blocker : 6  — logged only, no gate impact
Automatable    : 22 yes / 3 partial / 0 no
