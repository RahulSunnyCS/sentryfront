# QA Checklist

Generated by: qa-planner
Plan context: Realistic performance scoring — remove double-penalty, add CrUX field data, PSI best-practices category, UNAVAILABLE state, P2-07/P2-08 modules, consumer null-safety, XSS hardening, i18n
Risk level: MEDIUM
Auth/PII escalated: no

---

## Scoring — Double-Penalty Removal

🔴 A site with a PSI performance score of 0.82 and poor LCP is graded exactly B/82, not penalised further
  Scenario    : Feed `calculatePerformanceGrade` a `LighthouseMetrics` object where `performanceScore = 0.82` and `lcp = 5000`. The old code subtracted −0.15 on top of the already-weighted PSI score.
  Pass if     : `performanceGrade` is 'B' and `performanceScore` is `82` (integer). No additional LCP or CLS delta applied.
  Fail if     : Score is anything other than 82 (e.g. 67 from the old −0.15 penalty) or grade does not match.
  Automatable : yes
  @critical

🔴 The single ×100 conversion lives only inside `calculatePerformanceGrade`; `performance-suggestions.ts` no longer multiplies by 100 a second time
  Scenario    : Call `generateAIPromptBundle` (or the function that renders the Lighthouse score in the AI prompt) with `performanceScore = 0.85` (0–1 range). Check the rendered string.
  Pass if     : The rendered score in the prompt text is `85`, not `8500` or any other artefact of double-multiplication.
  Fail if     : The string contains `8500`, or any integer > 100 derived from that field.
  Automatable : yes
  @critical

🔴 A lab score of exactly 0.0 persists as integer 0, never as null
  Scenario    : Simulate a PSI response with `categories.performance.score = 0`. Run through the full path: parser → `calculatePerformanceGrade` → `scan-worker` Prisma update → API GET → response body.
  Pass if     : `performanceScore` in the API response is `0` (number), not `null`, not `undefined`, and `performanceGrade` is `'F'`.
  Fail if     : `performanceScore` is `null` or missing; or `performanceGrade` is absent.
  Automatable : yes
  @critical

🟡 Grade boundaries map correctly: score 90 → A, 89 → B, 80 → B, 79 → C, 70 → C, 69 → D, 60 → D, 59 → F
  Scenario    : Unit-test `calculatePerformanceGrade` at every boundary value with no LCP/CLS in the metrics (null).
  Pass if     : Each boundary returns the correct letter grade.
  Fail if     : Any boundary is off by one grade.
  Automatable : yes
  @functional

---

## CrUX Field Data — Parsing and Normalization

🔴 CLS CrUX percentile is normalized by dividing by 100: a raw `CUMULATIVE_LAYOUT_SHIFT_SCORE.percentile` value of 10 must produce a field CLS of 0.10
  Scenario    : Provide a PSI v5 JSON fixture with `loadingExperience.metrics.CUMULATIVE_LAYOUT_SHIFT_SCORE.percentile = 10`. Parse it through the updated `lighthouse.ts` field-data extraction path.
  Pass if     : The extracted field CLS value is `0.10` (float).
  Fail if     : The value is `10`, `0.01`, or any value other than `0.10`.
  Automatable : yes
  @critical

🔴 The field verdict uses Google's verbatim `overall_category` string only — the code never self-computes a Good/NI/Poor bucket from the raw percentiles
  Scenario    : Feed a PSI fixture where `loadingExperience.overall_category = 'NEEDS_IMPROVEMENT'`. Capture what the parser stores as the real-user verdict.
  Pass if     : The stored verdict is exactly the string `'NEEDS_IMPROVEMENT'` (or the direct passthrough of whatever the API returned).
  Fail if     : The code computes a different bucket label from the raw metric percentiles and stores that instead.
  Automatable : yes
  @critical

🟡 When `loadingExperience` is absent from the PSI response, the parser returns no field block and no default verdict is invented
  Scenario    : Provide a PSI response that omits `loadingExperience` entirely (key missing, not null). Run through the parser.
  Pass if     : The resulting metrics object has no field-data block (undefined or absent key), and no field verdict string.
  Fail if     : A default verdict string (e.g. 'SLOW', 'FAST') is synthesised from absence.
  Automatable : yes
  @functional

🟡 INP (`INTERACTION_TO_NEXT_PAINT`) absent from the PSI response is handled gracefully — the parser does not throw, does not substitute FID, and leaves the INP field absent
  Scenario    : Provide a PSI fixture with `loadingExperience.metrics` that contains no `INTERACTION_TO_NEXT_PAINT` key.
  Pass if     : Parser completes without error; the metrics object has no INP field or the field is explicitly null/undefined; no FID value appears in its place.
  Fail if     : Parser throws, or sets the INP field to the FID value.
  Automatable : yes
  @functional

🟡 `originLoadingExperience` is parsed into a separate context-only block and never promoted to the headline verdict or URL-level field block
  Scenario    : Provide a fixture where `originLoadingExperience.overall_category = 'SLOW'` but `loadingExperience.overall_category = 'FAST'`. Parse both.
  Pass if     : The headline/URL field verdict reflects `'FAST'`; the origin block is stored separately and labelled as context-only.
  Fail if     : The origin verdict overwrites or appears as the URL-level headline verdict.
  Automatable : yes
  @functional

---

## UNAVAILABLE State — PSI Failure Path

🔴 When PSI returns HTTP 429 (rate-limit), the scan records grade 'N/A', `performanceScore = null`, and `scoreSource = 'unavailable'` — never grade 'F' or score 0
  Scenario    : Mock the PSI endpoint to return HTTP 429. Run `runLighthouse` → `runPerformanceModules` through `scan-worker`.
  Pass if     : Persisted `performanceGrade` is `'N/A'`, `performanceScore` is `null`, and the `performanceMetrics` JSON contains `scoreSource: 'unavailable'`.
  Fail if     : `performanceGrade` is `'F'`, or `performanceScore` is `0`, or `scoreSource` is `'lab'`, or `scoreSource` is absent.
  Automatable : yes
  @critical

🔴 When PSI times out (AbortError), the result is UNAVAILABLE, not F/0
  Scenario    : Mock the PSI fetch to abort (simulate `AbortController` timeout). Run the full scan-worker path.
  Pass if     : `performanceGrade` is `'N/A'`, `performanceScore` is `null`, `scoreSource` is `'unavailable'`.
  Fail if     : Grade is `'F'` or score is `0`.
  Automatable : yes
  @critical

🔴 The UNAVAILABLE path persists a non-empty `performanceMetrics` JSON object — the object must contain at least `scoreSource: 'unavailable'`
  Scenario    : Trigger any PSI failure (429, timeout, network error). Inspect the value stored in `Scan.performanceMetrics` after the scan-worker update.
  Pass if     : `performanceMetrics` is a non-empty JSON string; parsed object contains `{ scoreSource: 'unavailable' }`.
  Fail if     : `performanceMetrics` is `null`, `'{}'`, or an empty string; or `scoreSource` is absent; or the scan-worker spread-guard skips the field entirely.
  Automatable : yes
  @critical

🔴 The UI displays "performance not measured (provider unavailable)" copy — not a grade badge — when `scoreSource` is 'unavailable'
  Scenario    : Render the performance section component with a `performanceMetrics` object where `scoreSource = 'unavailable'` and `performanceScore = null`.
  Pass if     : The UI shows the unavailable message string; no grade letter or numeric score badge is rendered.
  Fail if     : The component renders 'F' or '0' or throws on null score.
  Automatable : partial
  @critical

🟡 API GET `/api/v1/scans/[id]` surfaces `scoreSource: 'unavailable'` inside the parsed `performanceMetrics` object when the scan used the UNAVAILABLE path
  Scenario    : Seed the DB with a scan whose `performanceMetrics` JSON contains `scoreSource: 'unavailable'`. Call the API. Inspect the response body.
  Pass if     : `response.performanceMetrics.scoreSource` is `'unavailable'`.
  Fail if     : The field is stripped, or the response omits `performanceMetrics`, or `scoreSource` is absent.
  Automatable : yes
  @functional

---

## Persistence Round-Trip and Backward Compatibility

🔴 A pre-change persisted blob (no `scoreSource`, no field block) is read back safely — reader treats missing `scoreSource` as 'lab' and missing field block as none, without throwing
  Scenario    : Seed the DB with a `performanceMetrics` JSON string that matches the pre-change shape (only lab CWV metrics, no `scoreSource` key, no `loadingExperience` block). Call the API route and the report page.
  Pass if     : Both consumers return successfully; `scoreSource` defaults to `'lab'`; field-data section is absent or empty rather than throwing.
  Fail if     : Either consumer throws, or `scoreSource` resolves to a value other than `'lab'`, or the field block is populated with invented data.
  Automatable : yes
  @critical

🔴 A new-code partial blob (field block present, `scoreSource` key missing) resolves to an explicit safe state, not silent 'lab' assumption
  Scenario    : Seed the DB with a blob that contains CrUX field data but no `scoreSource` key. Read through the API and report path.
  Pass if     : The reader produces a deterministic explicit result (e.g. resolves `scoreSource` to `'lab'` as documented, or surfaces a clear fallback) — it must not silently produce contradictory state (e.g. field data shown with no source label).
  Fail if     : The page throws, or the field block is silently discarded, or the source label contradicts the presence of field data.
  Automatable : yes
  @critical

🔴 Full persistence round-trip: new-format performanceMetrics (with `scoreSource`, CrUX block, `bestPracticesScore`) survives serialise → Prisma write → Prisma read → JSON.parse → API response intact
  Scenario    : Run a scan that produces the new-format blob. Capture the value at scan-worker write time. Fetch the scan via GET /api/v1/scans/[id]. Compare.
  Pass if     : All new keys (`scoreSource`, `loadingExperience`, `bestPracticesScore`, `bestPracticesGrade`) present in the API response `performanceMetrics` match those written.
  Fail if     : Any key is dropped or mutated in transit; JSON.parse throws on the stored string.
  Automatable : yes
  @critical

---

## Consumer Null-Safety — Zero-Score and Null Guards

🔴 `GET /api/v1/scans/[id]/performance-suggestions` does NOT return HTTP 404 when `performanceScore` is 0 (a genuine lab score for a very slow site)
  Scenario    : Seed a completed scan with `performanceScore = 0`, `performanceGrade = 'F'`, non-null `performanceMetrics`. Call the performance-suggestions endpoint.
  Pass if     : Response is HTTP 200 with a suggestions payload.
  Fail if     : Response is HTTP 404 ("No performance data available") or HTTP 500.
  Automatable : yes
  @critical

🔴 The performance section UI component does not throw or render NaN/undefined when `performanceScore` is 0
  Scenario    : Render `<PerformanceSection>` with `performanceData.performanceScore = 0`. The falsy `>=` chain at `performance-section.tsx:104-109` used to evaluate all branches as false for score 0.
  Pass if     : The component renders the 'Moderate performance' or correct threshold message (score 0 is below 50, so "Performance needs attention" copy applies); no NaN badge; no throw.
  Fail if     : Component throws, renders nothing, or renders a blank score badge.
  Automatable : partial
  @critical

🔴 The PDF export (print path) renders correctly for a scan with `performanceScore = 0` and for `performanceScore = null` (UNAVAILABLE)
  Scenario    : Load the print page with (a) score 0 and (b) score null. Check the `<ScoreBar>` render.
  Pass if     : (a) ScoreBar renders with score 0; (b) ScoreBar is omitted entirely (the `performanceScore !== null` guard at `print-report.tsx:440`).
  Fail if     : (a) ScoreBar shows NaN or crashes; (b) ScoreBar renders with a null or 0 value.
  Automatable : partial
  @critical

🟡 `p4-05-mobile-seo` uses `performanceScore !== null` before the `< 0.9` comparison, preventing a false finding when score is null
  Scenario    : Run `runMobileSEOModule` with `metrics.performanceScore = null`.
  Pass if     : No performance-related finding is emitted by the module.
  Fail if     : Module throws or emits a finding based on null < 0.9.
  Automatable : yes
  @functional

---

## PSI Timeout Budget

🔴 Worst-case PSI call time (timeout × (retries + 1)) stays well below `SCAN_TIMEOUT_MS = 120000`
  Scenario    : Read `PAGESPEED_TIMEOUT_MS` and `MAX_RETRIES` from the updated `lighthouse.ts`. Assert `PAGESPEED_TIMEOUT_MS * (MAX_RETRIES + 1) <= 60000` (a defined hard bound chosen to leave margin under 120 s).
  Pass if     : The product is ≤ 60000 ms. With the planned values of 45000 ms and 0 retries the product is 45000.
  Fail if     : The product exceeds 60000 ms, or `PAGESPEED_TIMEOUT_MS` is still the old 60000, or `MAX_RETRIES` is still 1 (product 120000 = exactly SCAN_TIMEOUT_MS with zero margin).
  Automatable : yes
  @critical

🟡 A PSI timeout (AbortError at the new 45 s limit) triggers the UNAVAILABLE path and does not leave the scan stuck in PENDING or rethrow into scan-worker
  Scenario    : Mock the PSI fetch to abort after 1 ms (simulate timeout). Run the full scan-worker execution including the timeout wrapper.
  Pass if     : Scan completes with status COMPLETED (or TIMEOUT only if the overall scan timeout fires), `performanceGrade = 'N/A'`, security and other findings are still present.
  Fail if     : `runScan` throws uncaught, scan stays PENDING, or security findings are lost.
  Automatable : yes
  @functional

---

## PSI Best-Practices Category (P2-08)

🟡 `lighthouse.ts` appends `category=best-practices` to the PSI request and parses `categories['best-practices'].score`
  Scenario    : Inspect the URL built by the updated `runLighthouse`. Provide a fixture containing `categories['best-practices'] = { score: 0.92 }`. Capture the parsed `bestPracticesScore`.
  Pass if     : URL includes `category=best-practices` parameter; parsed `bestPracticesScore` is `0.92`.
  Fail if     : Parameter is absent from URL; or field is not parsed; or code references the removed `no-vulnerable-libraries` audit.
  Automatable : yes
  @functional

🟡 Failed best-practices audits are converted to P2-08 findings with category 'Best Practices', and the module is wired into `runPerformanceModules`
  Scenario    : Provide a PSI fixture with two failed best-practices audits. Run `runPerformanceModules`.
  Pass if     : `result.findings` contains findings with `moduleId = 'P2-08'` and `category = 'Best Practices'`; the count matches the number of failed audits.
  Fail if     : No P2-08 findings appear, or they have a different category string.
  Automatable : yes
  @functional

🟡 When a best-practices audit is absent from the PSI response (Lighthouse version difference), the parser skips it gracefully without throwing
  Scenario    : Provide a PSI fixture that entirely omits several best-practices audit keys. Run through the P2-08 parsing path.
  Pass if     : Parser completes; no findings for the absent audits; no throw or unhandled error.
  Fail if     : Parser throws on a missing audit key.
  Automatable : yes
  @functional

🟢 `bestPracticesGrade` and `bestPracticesScore` appear in the report UI under a "Best Practices" heading when the data is present
  Scenario    : Render the performance section with `performanceMetrics.bestPracticesScore = 0.85`.
  Pass if     : A Best Practices grade/score element is visible in the report.
  Automatable : partial
  @non-blocker

---

## P2-07 — Real-User Field Experience Finding

🟡 P2-07 emits a HIGH finding with a report banner ONLY when URL-level `loadingExperience.overall_category === 'SLOW'` AND lab `performanceScore >= 50`
  Scenario    : Set up metrics with `overall_category = 'SLOW'` and `labScore = 65`. Run P2-07 (or `runPerformanceModules` with the P2-07 module wired in).
  Pass if     : One HIGH-severity P2-07 finding is present.
  Fail if     : No finding appears, or severity is not HIGH, or a banner is not triggered.
  Automatable : yes
  @functional

🟡 P2-07 does NOT emit a HIGH finding or banner when `overall_category === 'SLOW'` but lab score is below 50 (lab already surfacing the problem)
  Scenario    : Set `overall_category = 'SLOW'` and `labScore = 45`.
  Pass if     : No HIGH P2-07 finding; at most an INFO finding.
  Fail if     : A HIGH finding or banner is emitted.
  Automatable : yes
  @functional

🟡 When only `originLoadingExperience` has `overall_category === 'SLOW'` but `loadingExperience` is absent, P2-07 emits at most INFO, never HIGH
  Scenario    : Provide a fixture where `loadingExperience` is absent and `originLoadingExperience.overall_category = 'SLOW'`. Any lab score.
  Pass if     : P2-07 either emits an INFO finding or no finding at all; severity is never HIGH.
  Fail if     : A HIGH finding appears.
  Automatable : yes
  @functional

🟡 P2-07 emits no finding at all when `loadingExperience` is absent
  Scenario    : Provide a PSI fixture with no `loadingExperience` key. Run P2-07.
  Pass if     : Zero P2-07 findings in the result.
  Fail if     : Any P2-07 finding (HIGH, MEDIUM, INFO) appears.
  Automatable : yes
  @functional

🟡 P2-07 emits no HIGH finding when `overall_category` is 'AVERAGE' or 'FAST', even with poor lab score
  Scenario    : Set `overall_category = 'AVERAGE'` and `labScore = 85`. Run P2-07.
  Pass if     : No HIGH P2-07 finding.
  Fail if     : A HIGH finding is emitted.
  Automatable : yes
  @functional

---

## P2-01 — INFO Finding for Poor Individual CWV

🟡 P2-01 emits an INFO finding when a single LAB CWV metric is in the Poor band even if the blended Lighthouse score is decent (e.g. overall 75 but CLS is 0.30)
  Scenario    : Provide `performanceScore = 0.75` (lab) and `cls = 0.30` (Poor threshold >= 0.25). Run the updated P2-01.
  Pass if     : An INFO-severity P2-01 finding mentions the poor CLS, in addition to any existing MEDIUM finding; the blended score is not lowered.
  Fail if     : No INFO finding is emitted for the individual poor CWV; or the score is altered.
  Automatable : yes
  @functional

---

## XSS Safety — Attacker-Influenced Fields

🔴 CrUX `overall_category` and per-metric `category` strings containing `<script>` payloads are escaped in the report UI and PDF — no raw injection
  Scenario    : Construct a fake PSI response where `loadingExperience.overall_category = '<script>alert(1)</script>'`. Run parsing, persist, render the report page and the Playwright PDF print path.
  Pass if     : The rendered HTML shows the literal string `&lt;script&gt;alert(1)&lt;/script&gt;` (React auto-escape) or similar escaped form. No script tag executes. No `dangerouslySetInnerHTML` is used on these fields anywhere in the report or PDF path.
  Fail if     : The script tag appears unescaped in the DOM; or `dangerouslySetInnerHTML` is added to any field sourced from PSI/CrUX/best-practices data in the report.
  Automatable : partial
  @critical

🔴 Best-practices audit titles and descriptions from PSI that contain `javascript:` URI or HTML tag payloads are escaped when rendered in the report
  Scenario    : Feed an audit title `"javascript:alert(1)"` and description `"<img src=x onerror=alert(1)>"` through `parseAudit` → P2-08 finding → report rendering.
  Pass if     : The rendered output shows the literal payload string, not an active link or executed handler. No `dangerouslySetInnerHTML` on these fields.
  Fail if     : The payload executes in any browser context, or `dangerouslySetInnerHTML` is used for these fields.
  Automatable : partial
  @critical

🔴 All new CrUX / best-practices string fields are length-capped before storage and before rendering — an attacker cannot persist a megabyte string in `performanceMetrics`
  Scenario    : Construct a PSI response where `overall_category` and several audit titles are strings of 10,000+ characters. Run through the full parse → persist → read path.
  Pass if     : The stored strings are truncated to the documented cap; the Prisma update and JSON.parse succeed; no out-of-memory or DB column overflow.
  Fail if     : The full oversized string is stored; or the Prisma write throws a column-length error; or parsing fails.
  Automatable : yes
  @critical

---

## PSI Fixture Validation

🔴 The committed real PSI v5 JSON fixture is valid and parseable by the updated `lighthouse.ts` parser without error — all new fields (`loadingExperience`, `originLoadingExperience`, `categories.best-practices`) are present and correctly typed
  Scenario    : Import the committed fixture file. Run it through the updated parser (not mocked). Assert every extracted field has the expected type and representative value.
  Pass if     : Parser returns a `LighthouseMetrics`-shaped object with non-null CrUX fields, a non-null `bestPracticesScore`, and no thrown error.
  Fail if     : Parser throws; any new field is null when the fixture has data for it; or the fixture itself is missing from the repo.
  Automatable : yes
  @critical

🟡 P2-07 and P2-08 produce expected findings when run against the committed fixture
  Scenario    : Run P2-07 and P2-08 against the committed fixture. The fixture must have `loadingExperience.overall_category = 'SLOW'` (or equivalent) and at least one failed best-practices audit.
  Pass if     : P2-07 emits a finding of the expected severity; P2-08 emits at least one 'Best Practices' finding.
  Fail if     : Either module emits no findings, or the fixture does not contain the expected data to drive these modules.
  Automatable : yes
  @functional

---

## i18n — New Strings in All Catalogs

🟡 Every new performance-section UI string (UNAVAILABLE message, field data labels, best-practices heading) has a corresponding key in all five message catalogs: en, hi, ml, es, de
  Scenario    : Extract all `t('key')` / `useTranslations` key references from the updated performance components and report UI. Cross-check against `messages/en.json`, `messages/hi.json`, `messages/ml.json`, `messages/es.json`, `messages/de.json`.
  Pass if     : Every referenced key exists in every catalog.
  Fail if     : Any key is present in `en.json` but absent from one or more of the other four catalogs.
  Automatable : yes
  @functional

🟡 A missing locale key in a non-English catalog does not break next-intl or crash the page — it degrades gracefully (falls back or shows the key)
  Scenario    : Temporarily remove one new performance key from `de.json`. Load the report page with locale=de.
  Pass if     : Page renders; next-intl either falls back to the English string or shows the key name; no uncaught exception or blank page.
  Fail if     : Page crashes with an unhandled error or renders blank.
  Automatable : partial
  @functional

🟢 All five locale catalogs use natural-sounding phrasing for the new UNAVAILABLE and field-verdict strings (no machine-translated placeholder text)
  Scenario    : Manual review of the five new-string entries in each catalog.
  Pass if     : Strings read naturally in each language.
  Automatable : no
  @non-blocker

---

## Backward Compatibility — Existing Tests

🟡 The existing `performance.test.ts` penalty-model tests are removed or updated to reflect the new no-penalty scoring — none of the old "applies −0.15 for POOR LCP" assertions remain as-is
  Scenario    : Run the full Vitest suite after the scoring change. Check that the old penalty tests are absent or rewritten (they currently assert the bug behaviour).
  Pass if     : No test in `performance.test.ts` asserts a non-PSI-accurate score (e.g. 80 from 0.95−0.15). All tests pass.
  Fail if     : Old penalty assertions remain and now fail (or are left passing because the bug was not removed).
  Automatable : yes
  @functional

🟡 `scan-worker.test.ts` and `scanner-index.test.ts` fixtures are updated to include the new `scoreSource` field and pass without amendment
  Scenario    : Run `npm run test` after implementation. Check these two test files specifically.
  Pass if     : Both suites pass. No TypeScript errors on the fixture shapes.
  Fail if     : Either suite fails due to missing `scoreSource` or changed `performanceMetrics` shape.
  Automatable : yes
  @functional

🟡 The `lighthouse.test.ts` suite is extended to cover CrUX parsing, best-practices parsing, and UNAVAILABLE state — no existing test is broken
  Scenario    : Run `npm run test` on the lighthouse test file after implementation.
  Pass if     : All existing tests pass; new tests for CrUX, best-practices, and UNAVAILABLE are present and passing.
  Fail if     : Existing tests fail; or new coverage areas are absent.
  Automatable : yes
  @functional

---

## UI / Report Display

🟢 The performance score badge in the report shows 'N/A' text or an explicit unavailable indicator, not a zero or blank circle, when `scoreSource` is 'unavailable'
  Scenario    : Render the score badge component with `score = null` and `scoreSource = 'unavailable'`.
  Pass if     : Badge shows a recognisable unavailable state (e.g. 'N/A' text, greyed ring).
  Automatable : partial
  @non-blocker

🟢 The report's performance section does not show a "field data" sub-section at all when `loadingExperience` is absent from `performanceMetrics`
  Scenario    : Render the performance section with a metrics object that has no `loadingExperience` key.
  Pass if     : No field-data card or CrUX section is visible.
  Automatable : partial
  @non-blocker

🟢 Badge or label wording distinguishes "Real-user data" (URL-level CrUX) from "Lab data" (Lighthouse) clearly in the report UI
  Scenario    : Manual review of the report UI with both field and lab data present.
  Pass if     : Labels are unambiguous to a non-technical reader.
  Automatable : no
  @non-blocker

🟢 The best-practices category score is visually distinct from the performance score in the report, not conflated into one gauge
  Scenario    : Render the performance section with both `performanceScore` and `bestPracticesScore` populated.
  Pass if     : Two separate score elements are shown with distinct labels.
  Automatable : partial
  @non-blocker

---

TIER SUMMARY
🔴 Critical    : 17 — all must pass at Automation Gate for Gate 2 to pass
🟡 Functional  : 21 — failures -> CONDITIONAL PASS at Gate 2
🟢 Non-blocker : 6  — logged only, no gate impact
Automatable    : 27 yes / 8 partial / 3 no
